version: 1.0.0
# Peripherals Configuration

peripherals:

  # I2C master — SDA=GPIO8, SCL=GPIO18
  # Shared by PCA9554A expander (0x3F), CST826 touch (0x15), STEMMA QT
  - name: i2c_master
    type: i2c
    role: master
    config:
      port: 0
      pins:
        sda: 8
        scl: 18
